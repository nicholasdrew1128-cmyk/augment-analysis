<<<<<<< HEAD
# Coffee Leaf Rust Generalization

The project studies the domain gap between clean, curated RoCoLe coffee leaf images and messier automatically collected CLR images. It benchmarks DenseNet201 classifiers, compares augmentation strategies, evaluates YOLOv8-based leaf segmentation outputs, and saves final model checkpoints.

Group members: 

Nicholas Drew, drew0158@umn.edu

Joseph Schaack, schaa140@umn.edu

Thang Pham, pham0503@umn.edu

## Project Summary

Most CLR classifiers perform well on clean research datasets, but deployment images are often blurred, noisy, poorly lit, and cluttered. This project asks whether augmentation and segmentation can help a DenseNet201 classifier trained on RoCoLe-style data generalize to a messier external CLR dataset.

Main findings:

- A DenseNet201 baseline trained on RoCoLe validates well on RoCoLe but drops on the external dataset.
- Individual augmentations such as random resized crop and rain/shadow-style perturbations improve external performance.
- A final aggressive augmentation mix improves external accuracy from 69.19% to 82.92%.
- YOLOv8 segmentation visually isolates leaves, but the segmented evaluation sets underperform the raw external images in these experiments.
- A complete model trained with both clean and external data reaches the strongest reported validation numbers, but it is not used as the fair domain-generalization benchmark because it trains on external data.

## Repository Layout

```text
.
|-- FinalReport.pdf
|-- README.md
|-- models/
|   |-- completemodelparameters.pth
|   `-- densenet_finetuned.pth
`-- notebooks/
    |-- augmentation_comparisons.ipynb
    |-- augmented_model.ipynb
    |-- baseline_model.ipynb
    |-- combined_performance.ipynb
    |-- complete_model.ipynb
    |-- data_examination.ipynb
    `-- segmentation_two_models.ipynb
```

## Artifacts

| Path | Description |
| --- | --- |
| `FinalReport.pdf` | Full project report with background, methodology, results, and references. |
| `models/densenet_finetuned.pth` | DenseNet201 state dict for the final augmented model used in benchmark evaluation. |
| `models/completemodelparameters.pth` | DenseNet201 state dict for the complete model trained with both RoCoLe and external CLR data. |
| `notebooks/data_examination.ipynb` | Initial dataset exploration and visual inspection. |
| `notebooks/baseline_model.ipynb` | Baseline DenseNet201 training on RoCoLe without segmentation or final augmentation strategy. |
| `notebooks/augmentation_comparisons.ipynb` | Single and combined augmentation experiments. |
| `notebooks/augmented_model.ipynb` | Training notebook for the final augmented benchmark model. |
| `notebooks/segmentation_two_models.ipynb` | Comparison of models trained/evaluated with original and YOLOv8-normalized leaf datasets. |
| `notebooks/combined_performance.ipynb` | Evaluation of the augmented model on raw and segmented external data. |
| `notebooks/complete_model.ipynb` | Training notebook for the final model that combines clean and messy training data. |

## Data

Datasets used in the experiments:

- `RoCoLeDataset`: clean Robusta coffee leaf images with healthy/rust labels.
- `CLRDataset`: messier automatically collected Coffee Leaf Rust images used as the external benchmark.
- `RoCoLeDatasetNormed`: segmented/normalized version of RoCoLe generated by the segmentation pipeline.
- `CLRDatasetNormed`: one selected segmented leaf per external image.
- `CLRDatasetNormedAll`: all segmented leaves per external image, evaluated at folder level.

Dataset contents located at:

```python
need open links to data path here
```

## Environment

The notebooks were developed for Google Colab with GPU acceleration. A local environment can also run the PyTorch portions if the datasets are available locally and the Colab-specific Drive/file calls are replaced.

Core Python packages:

```text
torch
torchvision
albumentations
numpy
pandas
scikit-learn
matplotlib
seaborn
pillow
kagglehub
ultralytics
```

For local use, create an environment, install the core dependencies, install the dataset at given link, and run the notebooks:

create virtual environment
```bash
python -m venv .venv
```
activate for linux/macOS
```bash
source .venv/bin/activate
```

activate for windows
```bash
.venv\Scripts\activate.bat
```
install dependencies
```bash
pip install torch torchvision albumentations numpy pandas scikit-learn matplotlib seaborn pillow kagglehub ultralytics
```

Install the CUDA-specific PyTorch build that matches your machine if you plan to train locally with a GPU.

## Recommended Workflow

The notebooks are arranged in the order the project was developed:

1. `notebooks/data_examination.ipynb`
- Inspect candidate datasets and visualize image quality differences.

2. `notebooks/baseline_model.ipynb`
- Train a frozen DenseNet201 classifier head on RoCoLe.
- Evaluate on RoCoLe validation data and the external CLR dataset.

3. `notebooks/augmentation_comparisons.ipynb`
- Compare individual augmentation techniques.
- Compare combined augmentation strategies.

4. `notebooks/augmented_model.ipynb`
- Train the final benchmark model using the selected augmentation mix.
- Save model parameters.

5. `notebooks/segmentation_two_models.ipynb`
- Evaluate original versus segmented/normalized datasets.
- Compare models trained on `RoCoLeDataset` and `RoCoLeDatasetNormed`.

6. `notebooks/combined_performance.ipynb`
- Load the augmented model checkpoint.
- Evaluate it on raw external images, single-leaf segmented images, and all-leaf folder-level predictions.

7. `notebooks/complete_model.ipynb`
- Train the complete model using both clean and external data.
- Save the final combined-data checkpoint.

## Model Architecture

The experiments use DenseNet201 from `torchvision.models` with a two-class classifier head:

```python
import torch
import torch.nn as nn
from torchvision import models

model = models.densenet201(weights=None)
num_features = model.classifier.in_features
model.classifier = nn.Linear(num_features, 2)

state_dict = torch.load("models/densenet_finetuned.pth", map_location="cpu")
model.load_state_dict(state_dict)
model.eval()
```

The final augmented model uses 384 x 384 inputs and this augmentation mix during training:

- Color jitter
- Rotation
- Random shadow
- Gaussian blur
- Gaussian noise
- Random brightness/contrast
- Random resized crop

The augmented and complete models fine-tune the final DenseNet block and normalization layers with a learning rate of `0.0001`.

## Reported Results

| Model configuration | RoCoLe validation accuracy | External accuracy | Notes |
| --- | ---: | ---: | --- |
| Baseline DenseNet201 | 90.68% | 69.19% | Trained on RoCoLe without the final augmentation strategy. |
| Segmented single-leaf experiment | under 60% | 67.13% | Segmentation alone did not improve generalization. |
| Final augmented model | 95.34% | 82.92% | Best fair benchmark for training on RoCoLe-style data only. |
| Augmented + segmented evaluation | 95.34% | 66.23% | Segmented external images underperformed raw external images. |
| Complete combined-data model | 95.70% | 100.00% | Trained with both clean and external data, so it is not directly comparable to the domain-generalization benchmark. |

The best augmentation-only benchmark improves external accuracy by 13.73 percentage points over the baseline.

=======
# Coffee-Leaf-Rust-Generalization

Repository dedicated to DL 2026 project

Group Members: Nicholas Drew and Joesph Schaak

## Data

Datasets used can be found at the following locations:

[RoCoLe Data](https://data.mendeley.com/datasets/c5yvn32dzg/2)

[Cyber Physical Data Collection System CLR Dataset](https://ieee-dataport.org/documents/coffee-leaf-rust-dataset)

## Project Goals

The aim of this work is to research methods for generalizing deep learning models to classify Coffee Leaf Rust, (*Hemileia vastatrix*). More details on the methods and motivations can be found in the associated [Report](FinalReport.pdf). 

## Code Overview

The code for this project was run in Jupyter Notebooks on Google Colab. The function of each is listed here.

[data_examination](notebooks/data_examination.ipynb): Examining different candidate datasets for our project.

[baseline_model](notebooks/baseline_model.ipynb): Comparing the performance of the two datasets with no additional augmentation or segmentation.

[augmentation_comparisons](notebooks/augmentation_comparisons.ipynb): Benchmarking the performance of several candidate augmentations and their combinations on the model.

[segmentation_two_models](notebooks/segmentation_two_models.ipynb): Benchmarking the performance of the segmeneted RoCoLe data on the baseline architecture.

[augmented_model](notebooks/augmented_model.ipynb): Training model with final augmentation combination on RoCoLe. Weights from this model are stored in [densenet_finetuned](models/densenet_finetuned.pth).

[combined_performance](notebooks/combined_performance.ipynb): Benchmarking the performance of the augmented model on the segmented external datasets.

[complete_model](notebooks/complete_model.ipynb): Final model training, which combines the augmentations from the augmented model and **both** datasets to attempt the most "deployable" version of the model. Weights are stored in [completemodelparameters](models/completemodelparameters.pth).

## Architectures

This project heavily relies on two major deep learning architectures for the classification task:

DenseNet201: The main architecture used for classification. Intially trained only the classification layer, but eventually expanded to the top normalization and dense layer.

YOLOv8: Used to build a segmentation pipeline for the RoCoLe leaf images.

## Tools

[Albumentations](https://www.mdpi.com/2078-2489/11/2/125): Heavily used for augmentation pipeline. Allows for fast and flexible use of a large variety of augmentation techniques.

[CVAT](github.com/opencv/cvat) (Computer Vision Annotation Tool): Used to annotate external dataset for training segmentation model.

[Ultralytics](https://github.com/ultralytics/ultralytics): Used to train YOLOv8 image segmentation model.

## Key Results

Baseline: Densenet201 architecture gets an internal validation accuracy of **90.68%** on the RoCoLe dataset, but falls to **69.19%** on the external dataset. Performance results as a consequence of the domain shift between the two datasets.

Augmentation: Aggressive augmentations mixed with increased image sizes increased external accruacy to **82.92%** (an approximately 13% increase from the baseline).

Segmentation: Segmentation did not improve external accuracy, dropping the accuracy to **66.23%**. Suggesting key contextual information is lost during segmentation.





>>>>>>> cfb7b709bc11741b7c47178aef06913e45818dbb
